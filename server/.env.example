# Copy this file to .env and fill in your own PostgreSQL connection string.
# .env is gitignored and should never be committed.

DATABASE_URL=postgres://postgres:postgres@localhost:5432/content_governance_checker

# Optional: port the Express server listens on (defaults to 3000).
# PORT=3000
